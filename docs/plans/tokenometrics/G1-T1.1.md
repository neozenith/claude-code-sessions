# T1.1: An operator can see a multi-block response counted once in session totals

> - **Gap:** [G1: Response-level token accounting](./G1.md)
> - **Index:** [README.md](./README.md)
> - **Next:** [T1.2](./G1-T1.2.md)

- [x] **Done**

After ingesting a session whose single response is logged as 3 content-block events that each repeat `output_tokens=100`, the session's reported total output tokens is `100`, not `300`.  _(tracer bullet)_

| | |
|--|--|
| Test | `tests/test_response_dedup.py::test_multiblock_response_counted_once` — build a fixture cache from an in-repo JSONL fixture, then `get_session_usage()` (public query) returns `total_output_tokens == 100` for that session |
| Implements | `schema.py` (add `request_id`, `stop_reason`, `is_response_head` + index, bump `SCHEMA_VERSION`), `cache.py` (`_parse_event` extracts `requestId`/`stop_reason`; `_annotate_responses` zeroes non-head usage; `_write_parsed` writes the columns) |
| Depends on | — |
| Refactor | extract `_USAGE_COLS` constant; pull group-by-requestId into a small pure helper for reuse by G8 |
