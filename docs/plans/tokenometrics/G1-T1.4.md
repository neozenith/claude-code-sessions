# T1.4: A synthetic assistant event (no requestId) is its own head

> - **Gap:** [G1: Response-level token accounting](./G1.md)
> - **Index:** [README.md](./README.md)
> - **Prev:** [T1.3](./G1-T1.3.md)

- [x] **Done**

An assistant event with `requestId == null` (e.g. `<synthetic>`) is treated as its own head and its usage is retained.

| | |
|--|--|
| Test | `tests/test_response_dedup.py::test_null_request_id_is_own_head` — via `get_session_events()` the null-requestId event is head; totals include it once |
| Implements | `cache.py:_annotate_responses` (null `request_id` branch) |
| Depends on | [T1.1](./G1-T1.1.md) |
