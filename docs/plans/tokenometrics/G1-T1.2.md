# T1.2: An operator can see exactly one response-head per requestId

> - **Gap:** [G1: Response-level token accounting](./G1.md)
> - **Index:** [README.md](./README.md)
> - **Prev:** [T1.1](./G1-T1.1.md)
> - **Next:** [T1.3](./G1-T1.3.md)

- [x] **Done**

For an ingested multi-block response, exactly one returned event has `is_response_head == true`; the rest are `false`.

| | |
|--|--|
| Test | `tests/test_response_dedup.py::test_one_head_per_request_id` — `get_session_events()` returns exactly one head among the blocks sharing a `requestId` |
| Implements | `cache.py:_annotate_responses` (head = last block); `backend.py:get_session_events` surfaces `is_response_head` |
| Depends on | [T1.1](./G1-T1.1.md) |
